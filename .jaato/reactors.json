{
  "version": 1,
  "_comment": "Manual generation cascade. manual_writer + (future) toc_discoverer each emit a typed payload; reactors render the markdown. The Walker spawns the sessions; markdown is a derived artifact of the typed payloads.",
  "rules": [
    {
      "id": "manual_writer-render-section",
      "enabled": true,
      "match": {
        "event_type": "agent.completed",
        "where": "agent_id == 'manual_writer'"
      },
      "action": {
        "script": "scripts/reactors/render_manual_section.py",
        "params": {}
      }
    }
  ]
}
